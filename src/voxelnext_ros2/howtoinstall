sudo apt update
sudo apt install -y python3-venv python3-dev build-essential

python3 -m venv .venv --system-site-packages
source .venv/bin/activate   (이때는 ros 꺼진 상태로 하는 게 안전)
pip install --ignore-installed colcon-common-extensions "empy==3.3.4"

export PYTHONNOUSERSITE=1   (이건 오직 '시스템 기본 패키지'와 '현재 활성화된 가상환경/ROS 워크스페이스 패키지'만 깔끔하게 바라보게 해주는 명령어라서, 여기선 venv만 바라보게 해줌. cbr에 저걸 박아뒀지만, cbr말고 다른 빌드가 필요한 복셀을 위해서 명시.)
python3 -m pip install --upgrade pip wheel "setuptools<80"
python -m pip install torch==2.3.1 torchvision==0.18.1 torchaudio==2.3.1 --index-url https://download.pytorch.org/whl/cu118
python -c "import torch; print(torch.__version__, torch.__file__); print('CUDA Available:', torch.cuda.is_available())"
python -m pip install spconv-cu118


cd src/voxelnext_ros2
python -m pip install -r requirements.txt
python -m pip install "coverage>=7,<8"
python -m pip install -e . --no-build-isolation




; alias cb='PYTHONNOUSERSITE=1 colcon build --symlink-install --cmake-args -DPython3_EXECUTABLE=$(which python)'
; alias cbr='PYTHONNOUSERSITE=1 colcon build --symlink-install --cmake-args -DCMAKE_BUILD_TYPE=Release -DPython3_EXECUTABLE=$(which python)'


